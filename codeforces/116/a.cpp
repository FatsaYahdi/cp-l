#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;

    int max_p = 0;
    int p;
    while (t--) {
        int a,b;
        cin >> a >> b;
    }

    cout << max_p << endl;

    return 0;
}